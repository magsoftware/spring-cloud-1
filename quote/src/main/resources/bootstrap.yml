spring:
  application:
    name: quote
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:7777
      fail-fast: true

logging:
  level:
    org:
      springframework: info